# ssh-telebot-admin
Telegram bot to manage Linux SSH users: create/delete accounts, set per-user max concurrent sessions (PAM limits), list online users by unique SSH sessions, and fetch client IPs. Built with Python and pyTelegramBotAPI.
